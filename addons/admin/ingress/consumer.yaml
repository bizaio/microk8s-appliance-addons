controller:
  electionID: consumer-nginx-leader
  ingressClassResource:
    name: consumer-nginx  # default: nginx
    enabled: true
    default: false
    controllerValue: "k8s.io/consumer-nginx-nginx"
  service:
    type: NodePort
    nodePorts:
      http: 30280
      https: 30200
